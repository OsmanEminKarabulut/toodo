import 'package:flutter/material.dart';
import 'package:toodo/product/widgets/toodo_tile_widget.dart';

class TileCardWidget extends StatelessWidget {
  const TileCardWidget({super.key, required this.toodoTileWidget});

  final List<ToodoTileWidget> toodoTileWidget;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
